#!/usr/bin/python3
""" Module that checks if object is an instance or an instance of class"""


def is_kind_of_class(obj, a_class):
    """ A function that returns true if obj is an instance"""

    return (isinstance(obj, a_class))
